session_name: dotfiles
start_directory: ~/dotfiles
windows:
  - window_name: editor
    panes:
      - nvim
  - window_name: config
    panes:
      - shell_command:
        - cd ..
        - cd .config
        - nvim
  - window_name: shell
    panes:
      -
